import React, { useState, useEffect } from 'react';

function WebPlayback({access_token}) {

   return (
      <>
        <p>Access Token: {access_token}</p>
      </>
    );
}

export default WebPlayback
