import React from 'react'

//supply it a playlist id and the token as a prop
//shows a list of songs, with an x button next to each song(only shown when "delete songs" option is selected)

function PlaylistView() {
  return (
    <div>PlaylistView</div>
  )
}

export default PlaylistView